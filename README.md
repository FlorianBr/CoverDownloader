# CoverDownloader

## What is it

Checks recursively all MP3s for missing cover images and tries to downloads the cover from last.fm

## How

- Create a last.fm API key at <https://www.last.fm/api/account/create>
- Add your key at the top of the python file
- If necessary install the mutagen package
- Start the program and specify the directory as a parameter
